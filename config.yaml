# set pipeline to run against either GRCh37 or GRCh38
BUILD:
  "GRCh38"

# set file directory of downloaded public data
PUBLICDIR:
  "/store/lkemp/publicData/"

# set temporary directory
TEMPDIR:
  "/store/lkemp/exome_project/tmp/"